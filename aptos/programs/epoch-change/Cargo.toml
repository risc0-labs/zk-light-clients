[package]
version = "1.1.0"
name = "epoch-change-program-builder"
edition = "2021"
license = "Apache-2.0"

[build-dependencies]
risc0-build = { workspace = true, features = ["unstable"] }

[package.metadata.risc0]
methods = ["guest"]
